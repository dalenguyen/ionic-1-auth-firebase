(function (window) {
  window.__env = window.__env || {};

  // API variable
  window.__env.apiKey = "your-apiKey";
  window.__env.authDomain = "http://dalenguyen.me";
  window.__env.databaseURL = "http://dalenguyen.me";
  window.__env.storageBucket = "http://dalenguyen.me";

  // Whether or not to enable debug mode
  // Setting this to false will disable console output
  window.__env.enableDebug = true;
}(this));
